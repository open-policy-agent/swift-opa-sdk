version: 1
builder:
  configs:
    # No swift_version pin: apply to every SPI builder toolchain. Pinning to a
    # version SPI doesn't run silently skips this config and builds no docs.
    - documentation_targets:
        - SwiftOPASDK
        - Runtime
        - Config
